## TIER IV universe / split mirror (perception)

This repository split mirrors `main` branch of `autowarefoundation/autoware_universe:/perception/` into `awf-latest` branch, used for TIER IV release workflows.

This branch only holds workflow files. See the beta branches for source codes.
